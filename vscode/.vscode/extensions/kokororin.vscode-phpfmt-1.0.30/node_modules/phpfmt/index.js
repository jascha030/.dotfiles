var path = require('path');
var fs = require('fs');

var phpfmt = {
  pharPath: path.join(__dirname, 'fmt.phar')
};

phpfmt.hasInstalled = function() {
  return fs.existsSync(phpfmt.pharPath);
};

phpfmt.default = phpfmt;

module.exports = exports = phpfmt;
