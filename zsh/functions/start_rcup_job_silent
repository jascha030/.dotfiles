#!/usr/bin/env zsh

# Silently start job to update dotfiles w/ RCM, calls `./rcup`
function start_rcup_job_silent {
    setopt LOCAL_OPTIONS NO_NOTIFY NO_MONITOR   

    sh $DOTFILES/rcup &

    disown &>/dev/null
}

