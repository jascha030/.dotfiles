# .dotfiles
Backup of my .dotfiles (zsh, bash, git, npm)

In case of new install.

```shell
brew install stow

cd ~

git clone (this repo)
```

then run `stow` on each subdirectory

```shell
cd ~/.dotfiles

stow zsh #example
```
